// AUTO-GENERATED by scripts/generate-wrappers.ts — DO NOT EDIT
import React, { forwardRef, useRef, useEffect } from 'react'

import '@nymbal/web-components'

declare global {
  namespace JSX {
    interface IntrinsicElements {
      'nymbal-add-to-cart': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-cart-drawer': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-mini-cart': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-variant-selector': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-quantity-selector': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-search-bar': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-product-filter': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
      'nymbal-toast': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement>
    }
  }
}

export interface AddToCartProps {
  variantId?: string
  disabled?: boolean
  onItemAdded?: (event: CustomEvent<{ variantId: string }>) => void
  children?: React.ReactNode
  className?: string
  style?: React.CSSProperties
}

export const AddToCart = forwardRef<HTMLElement, AddToCartProps>(
  function AddToCart(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.variantId !== undefined) {
      el.setAttribute('variant-id', String(props.variantId))
    } else {
      el.removeAttribute('variant-id')
    }
  }, [props.variantId])

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.disabled) {
      el.setAttribute('disabled', '')
    } else {
      el.removeAttribute('disabled')
    }
  }, [props.disabled])

  useEffect(() => {
    const el = ref.current
    if (!el || !props.onItemAdded) return
    const handler = props.onItemAdded as EventListener
    el.addEventListener('nymbal:cart:item-added', handler)
    return () => el.removeEventListener('nymbal:cart:item-added', handler)
  }, [props.onItemAdded])

    return (
      <nymbal-add-to-cart ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        {props.children}
      </nymbal-add-to-cart>
    )
  },
)

export interface CartDrawerProps {
  open?: boolean
  onCheckoutClick?: (event: CustomEvent<void>) => void
  className?: string
  style?: React.CSSProperties
}

export const CartDrawer = forwardRef<HTMLElement, CartDrawerProps>(
  function CartDrawer(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.open) {
      el.setAttribute('open', '')
    } else {
      el.removeAttribute('open')
    }
  }, [props.open])

  useEffect(() => {
    const el = ref.current
    if (!el || !props.onCheckoutClick) return
    const handler = props.onCheckoutClick as EventListener
    el.addEventListener('nymbal:cart:checkout-clicked', handler)
    return () => el.removeEventListener('nymbal:cart:checkout-clicked', handler)
  }, [props.onCheckoutClick])

    return (
      <nymbal-cart-drawer ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-cart-drawer>
    )
  },
)

export interface MiniCartProps {
  onClick?: (event: CustomEvent<void>) => void
  className?: string
  style?: React.CSSProperties
}

export const MiniCart = forwardRef<HTMLElement, MiniCartProps>(
  function MiniCart(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>



  useEffect(() => {
    const el = ref.current
    if (!el || !props.onClick) return
    const handler = props.onClick as EventListener
    el.addEventListener('nymbal:mini-cart:clicked', handler)
    return () => el.removeEventListener('nymbal:mini-cart:clicked', handler)
  }, [props.onClick])

    return (
      <nymbal-mini-cart ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-mini-cart>
    )
  },
)

export interface VariantSelectorProps {
  options?: unknown
  onVariantSelected?: (event: CustomEvent<{ variant: unknown }>) => void
  className?: string
  style?: React.CSSProperties
}

export const VariantSelector = forwardRef<HTMLElement, VariantSelectorProps>(
  function VariantSelector(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.options !== undefined) {
      el.setAttribute('options', JSON.stringify(props.options))
    } else {
      el.removeAttribute('options')
    }
  }, [props.options])

  useEffect(() => {
    const el = ref.current
    if (!el || !props.onVariantSelected) return
    const handler = props.onVariantSelected as EventListener
    el.addEventListener('nymbal:variant:selected', handler)
    return () => el.removeEventListener('nymbal:variant:selected', handler)
  }, [props.onVariantSelected])

    return (
      <nymbal-variant-selector ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-variant-selector>
    )
  },
)

export interface QuantitySelectorProps {
  value?: number
  min?: number
  max?: number
  onQuantityChange?: (event: CustomEvent<{ value: number }>) => void
  className?: string
  style?: React.CSSProperties
}

export const QuantitySelector = forwardRef<HTMLElement, QuantitySelectorProps>(
  function QuantitySelector(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.value !== undefined) {
      el.setAttribute('value', String(props.value))
    } else {
      el.removeAttribute('value')
    }
  }, [props.value])

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.min !== undefined) {
      el.setAttribute('min', String(props.min))
    } else {
      el.removeAttribute('min')
    }
  }, [props.min])

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.max !== undefined) {
      el.setAttribute('max', String(props.max))
    } else {
      el.removeAttribute('max')
    }
  }, [props.max])

  useEffect(() => {
    const el = ref.current
    if (!el || !props.onQuantityChange) return
    const handler = props.onQuantityChange as EventListener
    el.addEventListener('nymbal:quantity:changed', handler)
    return () => el.removeEventListener('nymbal:quantity:changed', handler)
  }, [props.onQuantityChange])

    return (
      <nymbal-quantity-selector ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-quantity-selector>
    )
  },
)

export interface SearchBarProps {
  placeholder?: string
  debounceMs?: number
  onSearchSelect?: (event: CustomEvent<{ product: unknown }>) => void
  className?: string
  style?: React.CSSProperties
}

export const SearchBar = forwardRef<HTMLElement, SearchBarProps>(
  function SearchBar(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.placeholder !== undefined) {
      el.setAttribute('placeholder', String(props.placeholder))
    } else {
      el.removeAttribute('placeholder')
    }
  }, [props.placeholder])

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.debounceMs !== undefined) {
      el.setAttribute('debounce-ms', String(props.debounceMs))
    } else {
      el.removeAttribute('debounce-ms')
    }
  }, [props.debounceMs])

  useEffect(() => {
    const el = ref.current
    if (!el || !props.onSearchSelect) return
    const handler = props.onSearchSelect as EventListener
    el.addEventListener('nymbal:search:selected', handler)
    return () => el.removeEventListener('nymbal:search:selected', handler)
  }, [props.onSearchSelect])

    return (
      <nymbal-search-bar ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-search-bar>
    )
  },
)

export interface ProductFilterProps {
  facets?: unknown
  onFilterChange?: (event: CustomEvent<{ name: string; value: string }>) => void
  className?: string
  style?: React.CSSProperties
}

export const ProductFilter = forwardRef<HTMLElement, ProductFilterProps>(
  function ProductFilter(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.facets !== undefined) {
      el.setAttribute('facets', JSON.stringify(props.facets))
    } else {
      el.removeAttribute('facets')
    }
  }, [props.facets])

  useEffect(() => {
    const el = ref.current
    if (!el || !props.onFilterChange) return
    const handler = props.onFilterChange as EventListener
    el.addEventListener('nymbal:filter:changed', handler)
    return () => el.removeEventListener('nymbal:filter:changed', handler)
  }, [props.onFilterChange])

    return (
      <nymbal-product-filter ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-product-filter>
    )
  },
)

export interface ToastProps {
  position?: string
  durationMs?: number
  className?: string
  style?: React.CSSProperties
}

export const Toast = forwardRef<HTMLElement, ToastProps>(
  function Toast(props, forwardedRef) {
    const innerRef = useRef<HTMLElement>(null)
    const ref = (forwardedRef ?? innerRef) as React.RefObject<HTMLElement | null>

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.position !== undefined) {
      el.setAttribute('position', String(props.position))
    } else {
      el.removeAttribute('position')
    }
  }, [props.position])

  useEffect(() => {
    const el = ref.current
    if (!el) return
    if (props.durationMs !== undefined) {
      el.setAttribute('duration-ms', String(props.durationMs))
    } else {
      el.removeAttribute('duration-ms')
    }
  }, [props.durationMs])



    return (
      <nymbal-toast ref={ref as React.LegacyRef<HTMLElement>} className={props.className} style={props.style}>
        
      </nymbal-toast>
    )
  },
)
